# ============ RAILWAY DATABASE ============
# Copy DATABASE_URL dari Railway Dashboard ke .env
# Railway → PostgreSQL → Variables → DATABASE_URL
DATABASE_URL=postgresql://user:password@host:5432/presensi

# ============ SERVER ============
PORT=3000
NODE_ENV=production

# ============ JWT SECRET ============
# Generate dengan perintah berikut:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-32-characters-or-more

# ============ EMAIL CONFIGURATION ============
# Gunakan Gmail dengan App Password
# Setup: https://myaccount.google.com/apppasswords
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# ============ CORS & BASE URL ============
# Sesuaikan dengan URL frontend/domain production
CORS_ORIGIN=https://yourdomain.com
BASE_URL=https://yourdomain.com

# ============ EXAMPLE UNTUK LOCAL DEVELOPMENT ============
# DATABASE_URL=postgresql://localhost/presensi
# NODE_ENV=development
# CORS_ORIGIN=http://localhost:8080
# BASE_URL=http://localhost:3000

